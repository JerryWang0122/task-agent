[project]
name = "task-agent-mcp-server"
version = "0.1.0"
description = "MCP tool layer for the Personal Task Management Agent tutorial"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "mcp>=1.0.0",
]

[project.optional-dependencies]
dev = []
