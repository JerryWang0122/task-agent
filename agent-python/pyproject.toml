[project]
name = "task-agent-python"
version = "0.1.0"
description = "Python Agent orchestration layer for the Personal Task Management Agent tutorial"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = []
